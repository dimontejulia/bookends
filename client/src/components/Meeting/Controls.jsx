import React from 'react';
import Button from '../Button';

export default function Controls() {
  return (
    <span>
      <Button>Toggle Camera</Button>
      <Button>Mute</Button>
      <Button>Leave Mtg</Button>
    </span>
  );
}
